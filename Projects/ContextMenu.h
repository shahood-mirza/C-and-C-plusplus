#include <stdio.h>
#include <conio.h>
#include <math.h>

#define TRUE 1
#define FALSE 0

//Function Prototypes
void ThevCalc(float r1, float r2, float r3, float r4, float volts);
float LoadVoltCalc (float loadInput, char formula);
float LoadCurrCalc (float loadInput, char formula);